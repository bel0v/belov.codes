{
  "name": "belov-codes-monorepo",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter website dev",
    "build": "pnpm --filter website build",
    "preview": "pnpm --filter website preview",
    "serve": "pnpm --filter website serve",
    "test": "pnpm --filter website test"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "5.9.3",
    "prettier": "3.6.2",
    "prettier-plugin-astro": "0.14.1",
    "vitest": "3.2.4"
  }
}
